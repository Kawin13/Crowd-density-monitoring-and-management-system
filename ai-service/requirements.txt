# Core web framework
fastapi>=0.109.0
uvicorn[standard]>=0.27.0
python-multipart>=0.0.9

# Computer vision — use headless build to avoid GUI dependencies on Windows Server
# If you need RTSP streams via FFmpeg, switch to opencv-python instead of -headless
opencv-python>=4.9.0.80

# AI / YOLO
# CPU-only PyTorch wheels: this service runs YOLOv8n on CPU, and the default
# PyPI torch/torchvision wheels bundle ~2GB of CUDA libraries that are never
# used here. Pointing at the CPU wheel index cuts the install from ~4-5GB
# down to ~300-400MB, which is required to fit within free-tier build/image
# size and RAM limits (Render, Railway, Fly.io free plans).
--extra-index-url https://download.pytorch.org/whl/cpu
ultralytics>=8.2.0
torch>=2.1.0
torchvision>=0.16.0

# Numeric
numpy>=1.26.4

# Image processing
Pillow>=10.2.0

# HTTP client (async)
httpx>=0.26.0

# HTTP client (sync, used by ultralytics internals)
requests>=2.31.0

# Async file I/O
aiofiles>=23.2.1

# Environment variables
python-dotenv>=1.0.0
